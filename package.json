{
  "name": "mqtt-websocket-bridge",
  "version": "1.0.0",
  "description": "MQTT over WebSocket bridge for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
